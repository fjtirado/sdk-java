document:
  dsl: '1.0.1'
  namespace: test
  name: echo-not-wait
  version: '0.1.0'
do:
  - runShell:
      run:
        shell:
          command: echo
          arguments:
            - ${"hello world not awaiting \(.full_name)"}
        await: false