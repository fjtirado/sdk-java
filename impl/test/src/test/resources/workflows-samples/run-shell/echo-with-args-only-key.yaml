document:
  dsl: '1.0.1'
  namespace: test
  name: echo-only-key
  version: '0.1.0'
do:
  - runShell:
      run:
        shell:
          arguments:
            - Hello
            - ${.firstName}
            - ${.lastName}
          command: echo
        return: all
